name: my_project
description: A new Flutter project.

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  url_launcher: ^6.0.9 # Verifique se há uma versão mais recente no pub.dev

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
